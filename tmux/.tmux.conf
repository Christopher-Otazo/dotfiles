set -g mouse on
set -g default-terminal "screen-256color"
set -g prefix C-a
unbind C-b
bind C-a send-prefix

bind | split-window -h 
bind - split-window -v
unbind '""'
unbind %

set -g status-bg black 
set -g status-fg cyan
